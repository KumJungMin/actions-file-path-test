{
  "name": "desktop",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prebuild": "yarn start",
    "start": "cd ./dist && touch desktop1.js.map  && find ./dist -type f -name '*.js.map' -delete"
  }
}
